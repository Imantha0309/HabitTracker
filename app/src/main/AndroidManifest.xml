<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabitTracker">

        <!-- Launcher: Welcome Activity (first screen when app opens) -->
        <activity
            android:name=".auth.MainWelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true" />

        <!-- Sign up -->
        <activity
            android:name=".auth.SignupActivity"
            android:exported="true" />

        <!-- Main app screen after login -->
        <activity
            android:name="com.example.habittracker.MainActivity"
            android:exported="true" />

        <!-- Hydration reminder receiver -->
        <receiver
            android:name="com.example.habittracker.ui.hydration.HydrationReceiver"
            android:exported="true" />

    </application>
</manifest>
